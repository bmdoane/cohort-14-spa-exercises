// Sandwich IIFE
// Creation of the named object
var Sandwich = (() => {

  // Return the public interface that other code can interact with
  return {
  	// This is the return of all parts of the IIFE structure
  };

})();

